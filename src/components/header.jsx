import React from "react";

const Header = (props) => {
  return (
    <header>
      <div className="container">
        <h3>Online test</h3>
        <i class="fas fa-clipboard-list"></i>
      </div>
    </header>
  );
};

export default Header;
